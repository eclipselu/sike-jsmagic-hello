{
  "name": "greet",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bin": {
    "greet": "./bin/greet.js"
  },
  "scripts": {
    "build": "babel src --output-dir lib",
    "watch": "babel src --output-dir lib --watch"
  },
  "dependencies": {
    "minimist": "^1.2.0"
  }
}
